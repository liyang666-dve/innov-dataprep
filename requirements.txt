# 自研部分核心依赖（01/02/03/登记）——按"克隆即用"最小集
numpy>=1.26
pandas>=2.0
pyarrow>=10.0
PyYAML>=6.0

# 可选：视频/模糊检查（03 --blur 需要；帧数核对用系统 ffprobe，无需安装）
# av>=12.0
# opencv-python-headless>=4.9

# 可选：Rerun 回放（Web 回放页 / 一键动作→回放 需要；VLM 标注只用标准库，无需新增依赖）
# rerun-sdk>=0.19

# 可选：台账 Excel 视图 / 开发测试
# openpyxl>=3.1
# pytest>=8.0